[user]
    name = Shawn Allen
    email = shawnbot@users.noreply.github.com

[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    lg = log -p
    pr = pull --rebase
    lsd = log --graph --decorate --pretty=oneline --abbrev-commit --all
    spull = !git stash save && git pull && git stash pop

[core]
    excludesfile = ~/.gitignore

[push]
    default = current

[credential]
    helper = osxkeychain
